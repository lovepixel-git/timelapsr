name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: |
          xcodebuild build \
            -project Timelapsr.xcodeproj \
            -scheme Timelapsr \
            -destination 'platform=macOS' \
            -skipPackagePluginValidation \
            CODE_SIGNING_ALLOWED=NO

      - name: Unit Tests
        run: |
          xcodebuild test \
            -project Timelapsr.xcodeproj \
            -scheme Test \
            -destination 'platform=macOS' \
            -only-testing:TimelapsrTests \
            -skipPackagePluginValidation \
            CODE_SIGNING_ALLOWED=NO
